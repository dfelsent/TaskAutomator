<%= form_for(@doilist) do |f| %>
  <% if @doilist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@doilist.errors.count, "error") %> prohibited this doilist from being saved:
      </h2>

      <ul>
      <% @doilist.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
 <br />
 <div align="center">
 <div class="field">
   <%= f.label 'Email' %><br />
    <%= f.text_field :myuserid, :placeholder => "ASCO email address"  %>
  </div>
 
  <div class="field">
   <%= f.label 'Password' %><br />
    <%= f.password_field :mypass, :placeholder => "Benchpress password" %>
  </div>
 
  <div class="field">
    <%= f.label(:mylist, t('DOI list')) %><br />
    <%= f.text_area :mylist, :placeholder => "Paste in the list of DOIs delivered to HWX from Cadmus' email (that is, in the form \"JCO.2013.48.7777.pdf\"). The items should be separated by spaces or new lines." %>
  </div>
 
 <div class="field">
    <%= f.label 'Numeric Month' %><br />
    <%= f.number_field :mymonth,  :placeholder => "ex: 4 for April 5th" %>
  </div>
  
   <div class="field">
    <%= f.label 'Numeric Date' %><br />
    <%= f.number_field :mydate, :placeholder => "ex: 5 for April 5th" %>
  </div>
    
     <div class="field">
    <%= f.label 'Numeric Year' %><br />
    <em>Enter the current year (e.g., 2014 or 2015)</em><br>
    <%= f.number_field :myyear %>
    </div>
  
  <div class="actions">
   <%= f.submit "Submit Your Entry to Benchpress", :class => 'btn btn-primary' %>
  </div>
</div>

<% end %>
